User clicks a song
       ↓
dispatch(currentSongPlaying(song))
       ↓
Redux updates state.currentSong = song
       ↓
localStorage.setItem("currentSong", JSON.stringify(song))  ← saved to browser
       ↓
USER RELOADS THE PAGE
       ↓
playerSlice.js runs → reads localStorage.getItem("currentSong")
       ↓
JSON.parse() converts string back to object
       ↓
Redux initialState.currentSong = savedSong  ← already has the song!
       ↓
MusicPlay.jsx renders → currentSongPlaying is NOT null → no crash ✅
